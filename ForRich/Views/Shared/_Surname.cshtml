

<h3>Input Name for your group or Surname for family:</h3>

<hr />
@using (Ajax.BeginForm("_EmailsForGroup", new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "create_group", HttpMethod = "POST" }))
{
    <input type="text" name="surname" />
    <button class="btn btn-lg">OK</button>
}
<hr />
<form id="email"></form>
